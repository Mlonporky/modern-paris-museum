# Agent and worklog note

The canonical project instructions are in `AGENTS.md` so Codex sessions can discover them automatically.

For every new session:

1. Read `AGENTS.md`.
2. Read only the newest approximately 120 lines or last two entries of `worklog.md`.
3. Continue from the newest `Next step`.
4. Append a concise worklog entry after every completed part or paused handoff.
5. Search older worklog entries only when the newest context is insufficient.

